{
  "dockerFile": "../Dockerfile",
  "remoteUser": "me",
  // TODO: stub support
  // "initializeCommand": "micropy install || (pip install -r requirements.txt && micropy install)",
  "extensions": [
    "ms-vscode.cpptools",
    "xaver.clang-format",
    "ms-python.python",
    "ms-python.vscode-pylance",
    "ninoseki.vscode-pylens"
  ]
}
